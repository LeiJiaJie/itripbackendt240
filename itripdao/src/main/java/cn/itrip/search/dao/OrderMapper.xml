<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.itrip.search.dao.OrderMapper">
    <select id="getItripUserListByMap" resultType="cn.itrip.search.beans.pojo.ItripHotelOrder" parameterType="ItripHotelOrder">
        select *  from itrip_hotel_order where orderNo = #{orderNo}
    </select>
</mapper>